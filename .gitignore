legacy/**
assets/**
artifacts/**

**__pycache__**

**venv**

Dockerfile

# Ignore local scratch inside .github (prompts etc.) but keep CI workflows
# committable — a bare `.github/**` would also exclude workflow files.
.github/*
!.github/workflows

.spack/**

tools/**

.headroom/**
.tokensave/**
.opencode/**
.serena/**