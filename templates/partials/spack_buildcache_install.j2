{# Binary-cache install dispatcher: local filesystem store vs OCI registry.
   The local body is byte-identical to the pre-oci rendering (moved verbatim
   into spack_buildcache_install_local.j2); oci code lives only in
   spack_buildcache_install_oci.j2 and is structurally unreachable in
   local mode. Mode comes from plan_context (env spack.buildcache.mode,
   overridable via --buildcache-mode). #}
{% if spack_buildcache_mode == 'oci' %}
{% include 'partials/spack_buildcache_install_oci.j2' %}
{% else %}
{% include 'partials/spack_buildcache_install_local.j2' %}
{% endif %}
